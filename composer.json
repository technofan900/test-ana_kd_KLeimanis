{
    "name": "school/simple-shop-student-version",
    "description": "PHP mini projekts programmu testēšanas pārbaudes darbam. Skolēniem pašiem jāveido Unit testi un jāveic QA testēšana.",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "symfony/var-dumper": "^7.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "start": "php -S localhost:8080 -t public",
        "test": "phpunit tests"
    }
}
